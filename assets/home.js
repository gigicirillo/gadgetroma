document.addEventListener('DOMContentLoaded',()=>{const progress=document.createElement('div');progress.className='scroll-progress';document.body.appendChild(progress);const revealTargets=[...document.querySelectorAll('.sec,.home-photo-wall,.hero-stats,.cluster-showcase,.product-photo-row,.product-grid,.cta')];revealTargets.forEach((el,i)=>{el.classList.add(i%3===0?'reveal-left':i%3===1?'reveal':'reveal-right')});document.querySelectorAll('.cluster-links,.product-grid,.hero-stats').forEach(el=>el.classList.add('stagger'));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in-view')}),{threshold:.12,rootMargin:'0px 0px -8% 0px'});document.querySelectorAll('.reveal,.reveal-left,.reveal-right,.stagger').forEach(el=>io.observe(el));const parallax=[...document.querySelectorAll('.home-photo-wall figure,.cluster-photo-art,.product-photo-row img')];parallax.forEach((el,i)=>{el.classList.add('parallax-layer');el.dataset.speed=(.035+(i%4)*.012).toFixed(3)});const tick=()=>{const y=window.scrollY;const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max>0?(y/max)*100:0}%`;document.body.classList.toggle('scrolled',y>24);if(!matchMedia('(prefers-reduced-motion: reduce)').matches){parallax.forEach(el=>{const r=el.getBoundingClientRect();const center=r.top+r.height/2-innerHeight/2;el.style.transform=`translate3d(0,${(-center*Number(el.dataset.speed)).toFixed(1)}px,0)`})}};tick();addEventListener('scroll',()=>requestAnimationFrame(tick),{passive:true});addEventListener('resize',tick);});